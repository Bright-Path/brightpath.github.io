{% macro alert(content, icons) %}
    <div class="bp-alert">
        <div class="bp-alert-icon-container">
            {% if content.icon -%}
                <img src="{{ content.icon }}" alt="" class="bp-alert-icon bp-alert-custom-icon" />
            {% else %}
                <img src="{{ icons.alert }}" alt="" class="bp-alert-icon bp-alert-standard-icon" />
            {%- endif %}
        </div>

        <div class="bp-alert-message">
            {% if content.title -%}
                <h2 class="bp-alert-title">{{ content.title }}</h2>
            {%- endif %}

            <div class="bp-alert-description">
                {{ content.description | markdown | safe }}
            </div>
        </div>
    </div>
{% endmacro %}
