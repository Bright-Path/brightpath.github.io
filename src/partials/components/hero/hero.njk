{% macro hero(title, subtitle, image, link_text, link_href, links, icons) %}
    <div class="bp-hero {% if not image %}bp-section-hero{% endif %}" {% if image %}style="background-image: url('{{ image }}')"{% endif %}>
        <div class="bp-hero-content">
            {% if title -%}
                <h2 class="bp-hero-title">{{ title }}</h2>
             {%- endif %}

             {% if subtitle -%}
                <div class="bp-subtitle">
                    {{ subtitle | markdown | safe }}
                </div>
            {%- endif %}


            {% if link_text -%}
                <a href="{{ link_href }}" class="bp-button">
                    {{ link_text }}
                    <img src="{{ icons.button_arrow }}" alt="" class="bp-button-icon" />
                </a>
            {%- endif %}
        </div>

        {% if links -%}
            <div class="bp-hero-contact">
                {% for link in links -%}
                    <div class="bp-hero-contact-content">
                        <img src="{{ link.image }}" alt="" class="bp-hero-contact-icon" />
                        <div class="bp-hero-contact-info">
                            {{ link.link_content | markdown | safe }}
                        </div>
                    </div>
                {%- endfor %}
            </div>
        {%- endif %}

    </div>
{% endmacro %}
